anthropic>=0.40.0
openai>=1.50.0
python-dotenv>=1.0.0
rich>=13.7.0
fastapi>=0.115.0
uvicorn>=0.30.0

# 卷四十四 K stage 2c++ · wish-8d6b76a6 · service_start 跨平台进程检测
psutil>=5.9.0

# 卷四十六 II · wish-43f705b8 · FTS5 中文 tokenizer · 修中文 recall_memory 0 命中
jieba>=0.42.0

# wish-4a6331b2 · OPUS 视觉能力 · 图片压缩
Pillow>=10.0.0

# 0.9.5 · Mac 启动器托盘 (pystray 菜单栏常驻 · 跨平台无害)
pystray>=0.19.0

# wish-41ed72ef · 文档上传 · PDF 文本提取
PyPDF2>=3.0.0

# wish-41ed72ef · 文档上传 · DOCX 文本提取
python-docx>=1.0.0

# wish-41ed72ef · 文档上传 · PPTX 文本提取
python-pptx>=1.0.0

# 卷五十八续 · lint_check 工具 · 抓"语法对逻辑错"的 bug (未定义名/未用导入/重定义)
ruff>=0.6.0

# 卷六十四续八 · 微信发媒体 (ilink_media · AES-128-ECB CDN 加密上传)
# 漏列 = 全新环境发图片/视频/文件报『缺依赖』·AI 会退而编本地链接 (127.0.0.1·手机打不开)
cryptography>=42.0.0

# 卷 · 客户档案 Excel 导入 (parse_table · .xlsx) · 缺则 CSV 仍可用·xlsx 会给"pip install"提示
openpyxl>=3.1.0

# 卷 · 客户档案上传端点 (FastAPI UploadFile/File 依赖) · 缺则 /import-preview 起不来
python-multipart>=0.0.9

# wish-fb6b7427 · 事项 B · Windows toast 系统通知 (纯 ctypes · 零传递依赖)
winotify>=1.1.0; sys_platform == "win32"

# 0.8.2 hotfix · 补回依赖 + 桌宠音效新依赖
# requests/qrcode: 微信渠道 (扫码登录 + 主动消息)
requests>=2.31.0
qrcode>=7.4.2
# playwright: 浏览器的手/眼 (browser_act/browser_fetch) · 驱动系统 Edge 无需下载内核
playwright>=1.40.0
# PyQt6: 桌宠本体 + 完成提示音 (可选·没装只失桌宠·主程序不受影响)
# 注: PyQt6 6.2+ 已把 QtMultimedia 合并进主 wheel (from PyQt6.QtMultimedia 直接通)
# 不要再加 PyQt6-QtMultimedia——该包名在 PyPI 不存在·加了会让 pip install 直接失败 (社区 7/31 反馈)
PyQt6>=6.6.0
